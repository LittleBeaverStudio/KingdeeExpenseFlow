---
name: kingdee-expense-flow
slug: kingdee-expense-flow
displayName: 金蝶云星空报销助手
version: 2.0.11
summary: 小河狸工作室出品：金蝶云星空费用/差旅报销全流程提交，员工不登录金蝶也能报销。
description: 小河狸工作室出品。金蝶云星空「费用/差旅报销全流程」提交 Skill：员工无需登录金蝶，在对话里把发票交上来，agent 用纯 WebAPI 完成「费用申请单 → 下推报销单 → 挂收票信息／传附件 → 提交审批」，差旅线结构同构。内置发票 OCR 清晰度与抬头校验、收票信息 vs 附件分流（行程单双算预警）、跨组织挂票硬拦截、收票池账号可见性隔离、**提交硬闸门（v2.0.11：收票 0 行 / 无发票云流水号 / 跨组织 三条硬拦，无任何绕过参数）**、按发票金额调低报销额（fit，唯一的合规出路）、单一登录入口（ValidateUser）、收票单权限配置指引、提交前体检、报销制度提醒。
license: 小河狸非转售许可 1.0（企业内部使用免费，转售收费需授权）
tags: [金蝶云星空, 费用报销, 差旅报销, 财务自动化, WebAPI, 发票]
metadata:
  version: 2.0.11
  author: 小河狸工作室
  tags:
    - 金蝶云星空
    - 费用报销
    - 差旅报销
    - 财务自动化
    - WebAPI
    - 发票
---

## 许可与使用范围

本 Skill 采用「小河狸工作室非转售许可 1.0」，完整条款见本目录 `LICENSE` 文件。

- ✅ **免费使用**：
  - 个人学习、研究或个人事务；
  - **企业内部使用**——某一组织（无论是否为营利单位）为**其自身**业务运营而使用，包括内部办公、内部财务与会计处理、内部管理与报表、内部系统集成；同一控股关联实体使用视同内部使用。
- ✅ 可为上述用途自行修改本 Skill。
- ❌ **须另行取得书面授权**：对外销售、出租、许可；集成进对外收费的产品或服务；托管 / SaaS / 代运营；打包进收费课程、付费社群或付费安装包；收费的实施与咨询培训；**以及利用本 Skill 向任何第三方交付成果或提供服务**（含代理记账、为客户提供账目或报表服务等）。
- 📌 **区分标准只有一条**：**是否以本 Skill 向本组织之外的第三方交付成果或提供服务**。仅为自身业务使用 → 免费；向第三方交付或服务 → 需授权。
- 📌 再分发须保留本许可、版权声明与来源：小河狸工作室 LittleBeaverStudio · https://littlebeaver.top

**商业授权申请**：https://littlebeaver.top （邮箱 yk.niu@outlook.com）


> ⚠️ 字段说明：`slug` / `displayName` / `version` / `summary` / `tags` 是 **SkillHub（skillhub.cn）发布**要求的顶层字段
> （CLI 校验必须有 `slug`+`version`+`displayName`，否则 `skillhub publish` 直接 die）；
> `name` + `metadata` 是 **agentskills** 规范要求的。两套并存，互不影响。

# 金蝶云星空报销助手

> 小河狸工作室出品 ｜ 覆盖「费用 / 差旅」两条报销线的**全流程提交**：申请单 → 下推报销单 → 挂发票 → 提交审批。

> **关于平台「需配置 API Key」标签**：本 Skill **不需要申请任何 API Key**。
> 它使用**你自己的金蝶云星空账号**提交单据，只填一次连接信息即可；
> 若已安装 `kingdee-data-exporter`，**两者共用同一份配置，不需要重复填写**，
> 也不必去任何开放平台申请密钥。只填**账套名称**即可，账套 ID 会自动解析。
> 配置方法与自检见下方「安装与配置」。

本 skill 处理**写操作**（提交单据、下推、创建收票单、上传附件、提交审批），与只读导出的 `kingdee-data-exporter` 互补。

## 安装与配置

### 1. 依赖

```bash
python -m pip install -r requirements.txt
```

本 skill 的 `helpers/` 全部是**纯标准库**（含 `recvin_link.py`），核心写票功能无需第三方包。

### 2. 连接配置：通常**不需要你做任何事**

本 skill **不单独保存账号密码**，也没有独立的配置项。只要机器上已经装好并配置过
[`kingdee-data-exporter`](https://github.com/LittleBeaverStudio/KingdeeDataExporter)，
本 skill 会**自动复用它的连接配置**，不需要重复填写。

确认是否已就绪 —— 在导出技能目录执行（五步全绿即可）：

```bash
python data_exporter.py --doctor
```

> 为什么本 skill 自己也要读一次凭据？因为它要做**写操作**（提交单据、下推、挂传附件），
> 而导出技能是**只读**的、不提供写接口。所以连接动作必须自己完成，
> 但读的是**同一份配置**，用户不需要多填任何东西。

### 3. 只有这几种情况才需要自己配

| 情况 | 做法 |
|---|---|
| 没装导出技能，想单独用本 skill | 写 `~/.workbuddy/kingdee/config.json`（见下方），只填账套名称即可 |
| CI / 无人值守环境 | 用环境变量 `KINGDEE_BASE_URL` / `KINGDEE_ACCTID`（或 `KINGDEE_ACCT_NAME`）/ `KINGDEE_USERNAME` / `KINGDEE_PASSWORD` |
| 已经在用导出技能的 `config.py` 旧写法 | **无需操作**，本 skill 同样兼容 |

自己配的话，写这个（`~/.workbuddy/kingdee/config.json`，技能目录之外，升级不覆盖）：

```json
{
  "base_url": "https://你的域名/k3cloud/",
  "acct_name": "账套名称",
  "username": "集成账号",
  "password": "密码"
}
```

不需要自己找账套 ID —— 只填账套名称，脚本会自动解析；想列出来看看就跑一次
（这个接口**免账号密码**）：

```bash
python data_exporter.py --list-datacenters
```

### 4. 连不上时

优先跑导出技能的 `--doctor`，它会逐步定位是配置、网络、账套、登录还是权限问题。

**WebAPI 白名单未配**是最常见的（报 `MsgCode 11`，**与账号密码无关**）：
让金蝶管理员在「基础管理 → 公共设置 → 参数设置 → 基础管理 → BOS平台 → WebAPI →
「允许调用WebAPI接口用户」」加入取数账号。

> ⚠️ 密码连续错约 5 次会锁账号，本 skill 不做自动重试。

## 四种单据与下推关系（formid 已实测/已确认）

| 单据 | formid | 下推目标 | 状态 |
|---|---|---|---|
| 费用申请单 | `ER_ExpenseRequest` | → 费用报销单 `ER_ExpReimbursement` | ✅ 已跑通 |
| 费用报销单 | `ER_ExpReimbursement` | （终态，提交审批后付款） | ✅ 已跑通 |
| 出差申请单 | `ER_ExpenseRequest_Travel` | → 差旅费报销单 `ER_ExpReimbursement_Travel` | ✅ 创建→提交→审核 已跑通 |
| 差旅费报销单 | `ER_ExpReimbursement_Travel` | （终态，提交审批后付款） | 🟡 实体结构与费用单**完全同构**；下推→挂票→改明细→写联动→传附件→体检**全通过**，仅 Submit 待实跑 |

> ⚠️ formid 拼写极敏感：`ER_ExpReimbursement`（正确，无多余 s）vs `ER_ExpenseReimbursement`（错误，曾因此误判"单据不存在"）。ExecuteBillQuery 查不到某单据时，先怀疑 formid 拼错或账号无该表单权限，不要直接认定"没部署"。

## 核心概念：报销单上"收票信息"与"附件"是两个位置（最易踩坑）

| 位置 | 单据体/接口 | 用途 | 参与金额校验？ |
|---|---|---|---|
| **收票信息** | 报销单 `RecInvInfo` 单据体 → 链接收票单 `IV_ReceivedInvoice` | **上传发票**（上传完自动进收票单） | ✅ 是，"发票金额≥报销金额"校验读这里 |
| **附件** | `AttachmentUpLoad`（一步挂单）/ `UploadFile`+`BOS_Attachment` Save | 行程单、补充说明等 | ❌ 否 |
| **联动表** | 报销单 `FReimbAndRecInvInfo` 单据体 | 「明细 ↔ 发票分录」对应关系（= UI「合并生成费用明细」的底层） | ❌ 否，但决定明细是否"与发票一致" |

- 校验报错"发票金额不允许小于报销金额" = 收票信息里**没有**有效的收票单（发票没传对位置，或被传到了附件）。
- `FEInvoiceEntity` 是报销单另一个发票子表，**不是**收票信息，历史单多为空，不要往这里填发票来绕过校验。

## 标准流程（费用申请单 → 报销单）

1. **提交费用申请单**：Save `ER_ExpenseRequest`（模板见 `references/field_cookbook.md`，含 F_TJKT_Base_qtr 项目必填等坑）。默认"仅保存暂存"，确认后再 Submit。
2. **下推报销单**：Push `ER_ExpenseRequest` → `ER_ExpReimbursement`（`TargetFormId` 必填，启用默认规则）。生成报销单（草稿 Z，BillNo 空）。
3. **发票先"找"再"建"**（顺序很重要）：
   - **优先查**：`helpers/recvin_link.py find <发票号码>` 在金蝶收票池 `IV_ReceivedInvoice` 里找收票单。
     ⚠️ **池子里有没有票，取决于"这张票有没有被采集过一次"** —— 别假设系统会自动把它收进来
     （每日"下载"要**电子税务局手机验证码**，无法自动化）。查不到就走下面第 5 条。
   - **查不到 → 让员工在报销软件里上传一次**（当天进池、当天有流水号）。**不要手工建票**：
     手工建票永远没有发票云流水号，v2.0.11 起会被提交硬闸门拦住（详见「📥 发票从哪来」）。
4. **把发票挂进「收票信息」**（**已打通，纯金蝶 WebAPI**，不必用发票云/客户端）：
   ```bash
   python helpers/recvin_link.py link <报销单FID> <收票单号>            # 追加
   python helpers/recvin_link.py link <报销单FID> <收票单号> --replace  # 覆盖
   python helpers/recvin_link.py clear <报销单FID>                     # 清空
   python helpers/recvin_link.py precheck <报销单FID>                  # 提交前体检
   python helpers/recvin_link.py set-contact <报销单FID> <员工号>       # 补往来单位
   python helpers/recvin_link.py submit <报销单FID>                    # 提交（带体检）
   python helpers/recvin_link.py verify <报销单FID> <收票单号>          # 一键自检
   ```
   写法要点：`Model = {"FID":.., "FRecInvInfo":[{"FRecInv":{"FBillNo":"<收票单号>"}}]}`
   —— 字段**必须放在 `FRecInvInfo[]` 数组行内**，放单据头会报 `实体不存在此属性！[EntityType：BillHead]`。
   详见 cookbook §4.2。
   - ⚠️ **防抢关联**：一张收票单只能属于一张报销单，挂到第二张会静默抢走原单的关联。
     `recvin_link.py` 已内置拦截（`--allow-steal` 才放行）。
   - ⚠️ 已审核单据同样可写，操作前先看单据状态，别误改历史单。
5. **让明细与发票一致（"合并生成费用明细"）**：明细行金额应 = 实际报销额，且**必须 ≤ 发票价税合计**。
   - 金蝶 18 个操作里**没有**「合并生成费用明细」这个可调 API —— 它只是 UI 选票时的联动动作，
     底层落到两处数据：① 明细行 `FReimbLinkInvCode`/`FRecInvBillNo`/`FInvNumber`/`FInvOpenDate` 打标；
     ② 联动表 `FReimbAndRecInvInfo` 逐条发票分录（`FRecInvFid`/`FRecInvEntryId`/`FInvAllAmt`）。
   - ```bash
     python helpers/expense_edit.py set-detail <FID> --entry <明细分录内码> --amount 8.00
     python helpers/expense_edit.py linkage <FID> --recv SPD00000002 --entry <明细分录内码>
     ```
   - `recvin_link.py set-contact` 要先跑：`ValidateFlag=true` 时 `FCONTACTUNIT` 是**必填**（MsgCode=11）。
6. **附件分流**：行程单/说明走 `AttachmentUpLoad`，**不要**传收票信息（会双算）。
   ```bash
   python helpers/expense_edit.py attach <FID> "D:/x/行程单.pdf"
   ```
   ⚠️ **`IsLast` 必须显式传 `true`** —— 漏传会被反序列化成 `false`，接口照样返回 `IsSuccess=true` 和一个
   `FileId`，但**内容根本没进文件信息表**（回环下载报「数据库文件信息表中不存在编码为 X 的文件信息」）。
   helper 已内置「上传后立刻回环下载比 md5」的校验，别只看 IsSuccess。
7. **提交审批**：`precheck <FID>` 体检 → `submit <FID>`（有阻断项会自动中止）。
   已实测走通：A(暂存) → **B(已提交)**，**提交后收票信息保持**。
8. （可选）付款阶段由财务在金蝶操作，本 skill 只负责到"提交审批"。
9. **收尾自检**：`python helpers/recvin_link.py verify <报销单FID> <收票单号>`
   —— 会同时校验报销单侧行数和收票单侧 `LINKBILLID/LINKIVNUMBER` 回写，全绿才算成功。
   再加一条 `python helpers/expense_edit.py check <报销单FID>` 看金额链条（报销 ≤ 发票、联动合计 = 发票合计）。

## 金额与费用项目口径（提交前必须与用户确认）

- **口径（skill 自定义，2026-09-22 修正）**：`报销金额 ≤ 发票价税合计` 是**本 skill 的提交前体检标准**，
  **不是金蝶的强制校验**。别把它说成"金蝶会报错"——见下条实测。
- 🔴 **本账套未启用「发票金额 ≥ 报销金额」强校验**（2026-09-22 全量实测，**推翻旧结论**）：
  取 1129 张报销单（费用 828 + 差旅 301）逐单核对，**712 张**存在「已挂发票 < 报销金额」，
  其中大量为「**报销十几万 / 收票 0 行 / 状态 C 已审核**」。View 直读子表逐单确认（非链接表推断）：
  | FID | 单号 | 组织 | 状态 | 报销金额 | 收票行数 |
  |---|---|---|---|---|---|
  | 100147 | FYBX20240607000002 | 101 | **C** | 139,824.65 | **0** |
  | 100130 | FYBX20240606000004 | 101 | **C** | 86,400.00 | **0** |
  | 100116 | FYBX20240604000008 | 104 | **C** | 68,786.00 | **0** |
  | 100105 | FYBX20240531000001 | 101 | **C** | 13,211.00 | **0** |
  → **一张发票都没有，也能保存、提交、审核通过到 `C`。**
  所以「昨天没传发票也提交到审核人」是**系统本来就不拦**，不是"校验被绕过"。
  ⚠️ 别再写「校验发生在审核环节」——审核环节同样不拦（上面 4 张都是 C）。
- 🔴 **`Submit` 永远返回 `IsSuccess=true`**：WebAPI 提交不校验金额、不跑 UI 表单插件。
  **"提交成功" ≠ "校验通过"**，不要拿 `Submit` 的返回值当金额合规的证据。
  金额是否合规**只能由 skill 自己算**（`precheck`），金蝶接口不会替你挡。
- ⚠️ **但"系统不拦"≠"可以乱报"**：金额不匹配的单在**实际财务流转中仍会被人工/制度退回**。
  `precheck` 的阻断是**有意为之的业务闸门**，且自 2026-09-22 起变成**提交内置闸门**（见下节
  「🔒 提交硬闸门」）—— **默认拦死，且 v2.0.11 起没有任何绕过参数**。
- **正常情况**：按**实际发生额**报销，明细金额跟发票走。申请单金额只是预估，
  实际发生可在报销时调整（例：申请 100 元招待 80 + 交通 20 → 实际发票 90 元招待 85 + 交通 5，
  **明细就按发票写 85 / 5**，必要时连**费用项目**一起改）。
- **发票 > 申请单**：先与用户确认按哪个金额提交，并**排查是否"多开"**。
  若属多开，**不能按发票金额**，必须按**实际付款金额**报销（发票仅作佐证）。
- **发票 < 申请单**：属正常下调，按发票金额提交（本次实测：申请 10.00 → 发票 8.00 → 报销 8.00）。
- **⚠️ 凡申请单 ↔ 发票不一致，一律先跟用户确认再提交**，不要自行拍板。
- **网约车**：必须同步提交**行程单**。识别到行程单就传**附件**；未识别到要**提醒用户财务可能驳回**
  （但不硬拦截，建议同步提交）。
  **行程单金额绝不能当作发票金额** —— 本环境系统会把行程单也识别成发票金额造成双算。

## 🔒 提交硬闸门（2026-09-22 新增 / **v2.0.11 起取消全部绕过开关**）

### 越狱是怎么发生的（机制，不是漏洞）
金蝶 WebAPI `Save` 的官方参数 `ValidateFlag`（官方原文：「设为 false 时不对数据合法性进行校验」）
是一个**总开关**：置 `false` 时**整批业务校验被跳过**，而「发票金额不允许小于报销金额！」
正属于这批校验。本 skill 的 `save()` 为了能写字段（否则会被必填项卡住）默认传 `false`
→ 于是**同一张 0 发票的差旅报销单**：

| 报文 | 结果 |
|---|---|
| `ValidateFlag=true` | ❌ `MsgCode=11`，Errors 含「发票金额不允许小于报销金额！」 |
| `ValidateFlag=false` | ✅ `IsSuccess=true`，生成单号，**`Submit` 也能到 `B` 审核中** |

⇒ **不是攻破了什么，而是金蝶把"关掉校验"的开关摆在了 API 参数里。** UI 走强校验、API 可关，
两者授权不同步，这中间的差就是"越狱"。

### 闸门判据（`recvin_link.py` 已实装，v2.0.11）

分两组，**全部硬拦**，命中任意一条即在发出 `Submit` **之前**中止。

**第一组 · 挂票前置闸门（`guard_recv_invoices`，`link` 阶段）**

| # | 判据 | 命中后果 |
|---|---|---|
| 1 | **收票单在系统里查不到** | 中止，不允许写入这条挂票关系 |
| 2 | **收票单来源组织 ≠ 报销单组织**（跨组织借票） | 中止 |
| 3 | **该收票单没有发票云流水号**（`FPIAOZONESERIALNUMBER` 为空） | 中止 |

> 判据 3 是用户 2026-09-22 明确要求的："**不能提交上传发票没有流水号的报销单**"。
> 流水号是"这张票真的进过发票云"的唯一凭证，也是**防重复报销的锚点** —— 没有它，
> 同一张票可以被反复挂到不同单上，系统不会识别。
> ⚠️ 手工建票（`FGENERATETYPE=' '`）**永远拿不到流水号**（挂单不补、事后重下也不补，已实测），
> 所以 `create_recv_invoice()` 建出来的票自 v2.0.11 起**已经提交不了**，只剩测试/历史补录用途。

**第二组 · 提交内置闸门（`submit()` 内无条件执行）**

| # | 判据 | 说明 |
|---|---|---|
| 4 | **金蝶自己的严格校验**（`strict_probe` → `ValidateFlag=true`） | 用金蝶的规则判，口径不会跑偏 |
| 5 | **skill 自算口径**（`precheck`） | 补第 4 道的**盲区**（见下）；`发票合计 < 报销金额` → block；**收票 0 行 + 报销金额 > 0 → block** |

> 判据 5 的"收票 0 行"就是用户要求的"**不允许提交没有上传发票的报销单**"。

- **没有任何运行时开关**：`--force`、`--allow-no-invoice`、`--allow-cross-org`、
  `--allow-no-piaozone` **全部已删除**，`precheck` / `link` / `submit` 的签名里也没有 `allow_*` 参数。
- 命中 → `submit()` 返回 `(False, {"MsgCode": "GUARD", "Errors": [...]})`，CLI 退出码 `3`；
  `link()` 命中 → `(False, {...})`，CLI 退出码 `2`。
- **唯一的例外通道是"改源码"**：`recvin_link.py` 顶部有
  ```python
  ATTACHMENT_ONLY_ORGS = ()   # 默认空 = 不允许任何组织走附件路线
  ```
  只有**明确以附件（不挂收票信息）为既有做法的组织**才在此登记（历史上只有 org 105 属于这类）。
  这不是给调用方留的开关 —— 要放行必须提交代码改动，等于必须走一次人工评审。

### ⚠️ 金蝶严格校验（判据 4）有盲区，**不能只靠它**（2026-09-22 实测）
`strict_probe` 是靠 `Save` 报文触发的，**会被"编辑锁冲突"打断**：
报 `MsgCode=4`「XXX使用业务单据…冲突，请稍候再使用」，此时 `errors` 里**根本没有发票金额那一句**
→ `strict_errors` 为空 → 该道判据**静默放行**。实测重试 2 次仍冲突（**同一账号"先 View 再 Save 探针"会自锁**）。
⇒ 所以判据 5（skill 自算）是**主力**，判据 4 只是"金蝶视角的加分证据"。**拿不到金蝶结论时，更要自己算。**

### 闸门的边界（别过度承诺）
- 闸门**只管"发票金额/发票来源"这一类风险**，**不管单据完整性**（必填项是否填全）。
  `strict_probe` 顺带发现的必填缺失只作提示打印，不阻断 —— 因为 `Submit` 本来就不校验必填。
- 闸门在**库与 CLI 两层**都生效（`submit()` 无条件跑、`link()` 无条件跑），且**没有参数可绕**；
  但**源码级绕过路径始终存在**：调用方直接 `kd._svc("Submit", ...)` 就跳过了。
  闸门防的是"**误用默认参数**"和"**图省事的越狱**"，不是"**恶意改代码**"。
- 验证闸门自身（零风险，不会真提交 —— 探针的 `Save` 放行，只卡 `Submit`）：
  ```python
  orig = kd._svc
  def trap(m, f, d, timeout=90):
      if m == "Submit":
          raise AssertionError("❌ 闸门失效：已发出 Submit 请求")
      return orig(m, f, d, timeout)          # 严格探针的 Save 走真实调用
  kd._svc = trap
  ok, st = kd.submit(fid, formid)            # 期望 ok=False, MsgCode="GUARD"，Submit 从未被调用
  ```

### ✅ 闸门已验证（2026-09-22，全打桩、零成本）

| 场景 | 结果 |
|---|---|
| 收票 **0 行** + 报销金额 > 0 | ✅ block，`submit` 返回 `MsgCode=GUARD`，**`_svc` 全程未被调用**（没发 Submit） |
| 挂了票、金额也对齐，但**票无发票云流水号** | ✅ block（"等于没上传发票"），同样不发起 Submit |
| 票有流水号但**跨组织**（票属 104 / 单属 101） | ✅ block，话术点明"提交成功也会被驳回为 D" |
| 发票 800 < 报销 1000（金额未对齐） | ✅ block，提示用 `expense_edit.py fit` 调低 |
| 同组织 + 有流水号 + 金额对齐（**对照组**） | ✅ `blocks = 0`，正常放行 |
| `link()` 挂「无流水号」票 | ✅ 在 **Save 之前**中止，**没有写库** |


## 💰 报销金额 > 发票金额：**按发票金额调低**（2026-09-22 新增，这才是正路）

**诉求**：员工申请/填了 1620，发票只齐到 983.84。金蝶 UI 拦「发票金额不允许小于报销金额！」。
**没有 `--force` 这种绕过参数（v2.0.11 已删）**。**正确动作 = 把报销金额改到发票金额。**

```bash
# 默认目标 = 该单收票信息的发票价税合计；默认 dry-run，只算不写
python helpers/expense_edit.py --travel fit 101746
python helpers/expense_edit.py --travel fit 101746 --apply        # 确认后真写
python helpers/expense_edit.py --travel fit 101464 --mode ratio   # 多行按比例（默认从末行冲减）
python helpers/expense_edit.py --travel fit 101746 --target 900   # 显式指定目标
python helpers/expense_edit.py --travel check 101746              # 写完复查
```

`fit` 会：① 算发票合计 → ② 按 `sequential`（**从最后一行往前冲减**，贴近 UI 的"按发票金额调整"）
或 `ratio`（按比例）分摊 → ③ 打印**逐行 旧→新 + 税额联动 + 差旅费金额**对照表 → ④（`--apply` 时）逐行写。
**税额按同比例缩放**（保持税率不变），否则不含税会算错。

### 🔴 金额字段的勾稽关系（2026-09-22 实测，101731 六行 + 101464 十二行逐行吻合）

```
含税 ExpenseAmount  =  不含税 TaxSubmitAmt  +  税额 TaxAmt
                    =  LOCNOTAXAMOUNT（不含税）
                    =  FTravelAmount（差旅费金额，差旅单专有）
                    =  ExpSubmitAmount / LocExpSubmitAmount
```

| View 键 | Save 键 | 含义 | 实测（101731 行1） |
|---|---|---|---|
| `ExpenseAmount` | `FExpenseAmount` | **含税**（价税合计） | 234.0 |
| `TaxAmt` | `FTaxAmt` | 税额 | 19.32 |
| `TaxSubmitAmt` | `FTaxSubmitAmt` | **不含税**（名字反着起的，别被"Submit"骗） | 214.68 = 234 − 19.32 |
| `LOCNOTAXAMOUNT` | `FLOCNOTAXAMOUNT` | **不含税** | 214.68 |
| `FTravelAmount` | `FTravelAmount` | **差价旅费金额 = 含税**（差旅单必写） | 234.0 |

> ⚠️ 旧版把 `FLOCNOTAXAMOUNT` 误写成含税值、并**整个漏掉** `FTravelAmount`
> → 差旅单会报「差旅费金额不等于税额加费用金额」，且不含税字段被污染。**已修**（v2.0.9）。
> v2.0.10（2026-09-22）：推翻「手工建票兜底／异步补号」，定稿次日归集后报销；
> 补入员工端权限实测（可见性按**来源组织**切）与三条路线取舍。
> **v2.0.11（2026-09-22）：闸门从"可显式绕过"收紧为"硬拦截"，删除全部运行时开关**
> （`--force` / `--allow-no-invoice` / `--allow-cross-org` / `--allow-no-piaozone`），
> 并删除 `LoginByAppSecret` 第二登录口（只保留 `ValidateUser`）。
> 例外通道仅剩**改源码**登记 `ATTACHMENT_ONLY_ORGS`。
> ⚠️ 差旅费金额的键是 **`FTravelAmount`**，不是 `FExpTravelAmount`（旧文档写错，已订正）。

### 🔴 方向不对称：**调低安全，调高受限**（2026-09-22 实测）

| 方向 | 结果 | 依据 |
|---|---|---|
| **调低**（报销 2474.76 → 发票 1606.76） | ✅ 安全，不会踩校验 | 本次 101464 dry-run |
| **调高**（在报销单上加钱） | ❌ 被拦：**「关联费用申请金额超出源单已下推报销金额！」** | 实测 101464 / 101404 写入失败 |

→ 想"多报"只能**回出差申请单加额度**，别在报销单上加。所以 `fit` 对"目标 > 发票合计"
（有票时）或"无票时目标 > 当前额"一律**硬拒**。

### 与提交硬闸门的分工
- `fit` = **修数据**，让单据变合规 → 之后 `precheck` 自然通过，`submit` 畅通。
- 旧版的 `--force` = **关校验**，单据依然不合规 → 审核环节会被退 —— **v2.0.11 起已彻底删除**。
- **只有 `fit` 这一条路**：把报销额改到发票合计，或让员工把票补齐进收票池。

### 🔴🔴 写入的副作用：**成功的 `Save` 会让「暂存(Z)」单变成「已提交(A)」**（2026-09-22 实测）

| 单据 | 我的写入 | 写入后状态 |
|---|---|---|
| `101401` | ✅ **成功** | **Z → A**（并分配单号 `CLFBX00000001`） |
| `101464` | ❌ 失败（反写超限） | 仍 `Z` |
| `101404` | ❌ 失败（反写超限） | 仍 `Z` |

⇒ 差别就在**写入成功与否**。`set_detail` / `fit` 虽然只改金额，但会把单据**推进状态**，
而 `A` 之后 **WebAPI 撤不回**（`UnSubmit` 不存在、`UnAudit` 报"已关联工作流实例"）。

**因此两条纪律：**
1. **绝不要拿别人的草稿单做写入测试。** 要验证写入：**自建一张测试单**（`Save` 新建 → `Z`），
   测完 `Delete`（`Delete` 只放 `Z`/`A`/`D`）。
2. 对真实单跑 `fit --apply` 前，确认**该单本来就该进提交态**，或先让用户点头。

> 想"只算不写"就用默认的 dry-run（`fit` 不带 `--apply`）——**dry-run 是纯读，不触发任何状态变化**。

## 📥 发票从哪来：员工先在软件里上传一次（2026-09-22 定稿）

### 先说清事实：发票云流水号只在「采集那一刻」产生

| 上传方式 | 结果 | 实测 |
|---|---|---|
| 发票云 **手机小程序**上传 | ❌ 后台**依然没有**流水号 | 用户 2026-09-22 实测 |
| 把 PDF 传到收票单**附件** | ❌ 流水号仍为空 | 本 skill 实测（传完等 25s 仍空） |
| **手工建收票单 → 挂到同组织报销单** | ❌ **不会补号** | 旧结论「异步补号」已推翻 —— 用户实测：**事后从电子税务局重新下载，仍无流水号** |
| **员工在报销软件里上传**（走一次流程） | ✅ **有流水号** | 员工端上传链路实测正常（钱八 24 张 100% 有号，`GEN='3'`） |
| 管理员在**金蝶云星空里点「下载」**（电子税务局来源） | ✅ 有流水号 | 归集账号 `16394` 建的票 **100% 有号**（`GEN='3'/'4'`） |

⇒ **"手工建票兜底"不成立**（两路独立证据见 `references/field_cookbook.md` §3.3）。
⇒ 流水号只在**采集那一刻**产生 → **唯一稳定的自动化无关路径 = 员工自己在软件里上传一次**。

### 🔴 每日"下载昨日发票"**无法自动化**（2026-09-22 用户确认）

金蝶云星空里的每日下载，**每次都要电子税务局账号的手机验证码** —— 没有验证码就下不来。
所以"定时任务每天自动归集"这条路线**不可行**，不要再设计它。
（agent 能做的只是**消费已经进池的票**，不能替人过验证码。）

### ✅ 定稿路线：员工上传一次 → 拿到流水号 → 后续全交给 agent

```
员工：在自己的报销软件里把发票上传一次（拿流水号，这一步必须人工）
  ↓
agent：按发票号 resolve 找到权威票 → link 挂到报销单收票信息
  ↓
agent：fit 对齐金额 → precheck 体检 → submit 提交
```

> ⏳ **待用户实测确认**（决定这条路线成不成立）：在软件里**只上传、不保存**，
> 系统**能不能就识别到流水号**？
> · 能 → 员工只需"点一下上传"，不产生半张废单，体验最好，就走这条；
> · 不能 → 员工要走到"保存"那一步才产生流水号，那就要接受"员工端会多出一张收票单"。
> 这两天的实测结论回填到本节。

**为什么必须"有流水号"才能提交**（v2.0.11 起是硬闸门，见下节「🔒 提交硬闸门」）：
`FPIAOZONESERIALNUMBER` 是发票云**防重复报销**的锚点（发票云按 `expenseStatus`
未用/在用/已用 锁票，一张票只能用一次）。没有流水号 = 发票云不知道这张票被报销了
→ **同一张票可以被报两次**，且金蝶本身不校验发票号重复。所以"无流水号的单"必须挡在提交前。

### 三条路线（2026-09-22 实测后重排）

| 维度 | **路线1：中央 agent + 服务账号**（⭐ 推荐） | **路线2：员工端 + 精准数据规则** | **路线3：员工端用服务账号**（❌ 已否决） |
|---|---|---|---|
| agent 跑在哪 | **一处**（你的机器 / 公司服务器） | 每个员工自己的电脑 | 每个员工自己的电脑 |
| 高权限凭证放哪 | **只存一处，绝不下发** | 员工**自己的**账号（低权限） | ⚠️ 服务账号被复制到 N 台机器 = **全公司都拿到管理员账号** |
| 员工前端能看到池票 | ❌ 看不到（数据规则**不动**） | ✅ 能看到「自己 + 归集账号」的票 | ✅ 全都能看到 |
| 员工能看到同事私有票 | ❌ 看不到 | ❌ 看不到（规则里排除掉） | ✅ 全都看得到 |
| 抢票风险 | 无（agent 按发票号精确匹配 + 查重） | ⚠️ 有（池票无归属标注，两人可挂同一张） | ⚠️ 高 |
| 员工操作次数 | 1 次（在软件里上传发票，拿流水号） | 1~2 次（还要自己进金蝶选票） | 1 次 |
| 时效 | **当天**（员工上传后池里就有票） | 当天 | 当天 |

> **路线3 否决是对的**：把高权限账号分发到每台员工电脑 = 权限失控（而且金蝶账号登录后在
> `~/.workbuddy/kingdee/profiles.json` 一类位置**明文落盘**，等于把管理员口令贴到每台机器上）。
>
> **必须说清的技术硬事实**：金蝶的**数据规则对 UI 和 WebAPI 一视同仁** ——
> 实测钱八用 WebAPI 能 `View` 打开**同事的**收票单（84 个字段全返回）、能查到 18 个制单人的票。
> 所以**同一个账号，不存在"agent 能看、前端看不了"**。要同时满足
> 「智能体取得到票」+「员工前端看不到池子」，只有两条真出路：
> 1. **身份分离**（路线1）：agent 用服务账号、员工账号权限**不动** → 员工前端自然看不到池子；
> 2. **系统层面禁止该账号网页登录**（若金蝶版本支持"该用户仅允许 API 登录"），再把它用于 agent。
>
> 如果坚持"把能力签发给员工端"，就只能走**路线2**，并且要**放弃"前端看不到归集票"**这个诉求
> （前端会看到归集票，只是看不到同事的私有票）。
> ⚠️ **路线2 还有个大坑**：把「只能查看自己单据」**整个关掉** ≠ 只多看到归集票，
> 实测是**整组织可见**（钱八能看到组织 101 下 **18 个制单人**的票，含同事私有票）。
> 要做成路线2，数据规则必须写成"**或**"：`FCreatorId = 16394 或者 FCreatorId = 当前用户`；
> 若该版本写不出"或"，路线2 不成立。

### 路线1 的实装命令（agent 用服务账号跑，员工零操作）

```bash
# 1) 按发票号解析「该用哪张收票单」（同号多条时选：有流水号 + 未占用）
#    命中退出码 0，未命中 2；输出会给出建议用的那张 SPD 单号
python helpers/recvin_link.py resolve 24000000000000000001

# 2) 池里没有 → 先查重；⚠️ 不要"手工建票兜底"（手工建的票永远没流水号，事后补下载也不回填）
python helpers/recvin_link.py find 24000000000000000001     # 按发票号码（位置参数）
python helpers/recvin_link.py find --bill-no SPD00008770    # 按收票单号

# 3) 挂票：把收票单号挂到报销单的收票信息（多个用逗号分隔）
python helpers/recvin_link.py link 101746 SPD00008770
#    🔒 挂「没有发票云流水号」的票会被**硬拦**（v2.0.11 起无逃生口）：
#       让员工先把这张票上传一次拿到流水号，再来挂。

# 4) 金额对齐 → 体检 → 提交（闸门强制）
python helpers/expense_edit.py --travel fit 101746           # 报销额调到发票额
python helpers/recvin_link.py precheck 101746
python helpers/recvin_link.py submit 101746
```

> `link` 只剩一个默认**拦死**的显式口子：`--allow-steal`（抢别人已占用的票）。
> 跨组织、无流水号、无收票信息这三条**都是硬拦，没有参数可绕**。

### ❌ 已评估否决：`LoginByAppSecret`（第三方系统登录授权）

曾计划给 agent 配一套「应用ID + 应用密钥 + 集成用户」的专用凭证，**已评估放弃，代码也回退了**：

| 否决理由 | 说明 |
|---|---|
| **落地成本高于收益** | 金蝶侧的「集成用户」要**额外建账号 + 后台配授权 + 可能还要白名单**，用户明确表示「不现实、没有多余账号」 |
| **会让 skill 出现两个登录口** | 一旦支持 `app_id`，就有两套配置键、两条登录路径 → **登录口必须只有一个**。正常形态是：**复用 `kingdee-data-exporter` 的配置 + `ValidateUser` 口令登录** |
| 未实测 | 该分支当时是照官方文档写的，本环境没有 AppID 可验 |

⇒ **v2.0.11 起：`recvin_link.py` 只有一条登录路径**（`AUTH_SUFFIX` / `ValidateUser`），
`app_id` / `app_secret` / `KINGDEE_APP_ID` / `KINGDEE_APP_SECRET` 全部移除。
配置统一走 `~/.workbuddy/kingdee/config.json`（`base_url` / `acctid` / `username` / `password`）
或环境变量注入。

> 那"员工前端看不到池子"怎么办？**靠 agent 跑在中心、用有权限账号做事**：
> 员工不接触金蝶，发票交给 agent 处理；员工的账号权限**一个字都不改**。
> 这是路线1（见上表），不需要任何新凭证形态。

### 🔴 收票池可见性：按「来源组织」切，**不是**按制单人（2026-09-22 实测三组对照）

| 账号 / 状态 | 可见收票单 | 判定 |
|---|---|---|
| 取数账号 `13800000000` | **2000 张 / 23 个制单人 / 组织 101+104+105** | 全池 |
| 员工钱八 `598079`（**改权限前**，勾着"只能查看自己单据"） | **24 张 = 她自己的** | 仅自己 |
| 员工钱八 `598079`（**关掉"只能查看自己单据"后**） | **2000 张 / 18 个制单人 / 组织 101 全可见** | ⚠️ **整组织可见**，**同事的私有票也露出来** |

**判别字段是 `FSOURCEORGID`（来源组织）**：同一位制单人（`16394`）建的单 ——
组织 101 的 `SPD00008766 / 08767 / 08768 / 08769 / 08770`（即那 5 张"缺票"）钱八**5/5 可见**；
组织 104 的 `SPD00008771 / 08772 / 08773` **不可见**。

于是：
- ✅ 用户问的「那 5 张钱八能否看到」→ **能，5/5 都能**（它们都是组织 101）。
- ⚠️ **代价**："把'只能查看自己单据'关掉" ≠ "只多看到归集票"，而是**把同组织同事的票全放出来**
  （样本 6/6 可见，且能 `View` 打开同事的单、84 个字段全暴露）→ **不满足"前端看不到别人的票"**。
- ✅ **报销单没有被连带放开**：钱八仍只看得到自己的 2 张差旅单，`View` 别人的单报
  `MsgCode=11`「您没有单据编号为…的差旅费报销单的权限！」

**复现这一切只要一条命令**（`helpers/perm_probe.py`，**纯只读**）：

```bash
# 当前（默认）身份的基线
python helpers/perm_probe.py --pool --bills --by-invoice 24000000000000000001

# 换成员工身份再跑一遍（环境变量注入，别改全局 config）
KINGDEE_USERNAME=钱八 KINGDEE_PASSWORD=*** python helpers/perm_probe.py --pool

# 结果落盘做两次身份对照
python helpers/perm_probe.py --pool --top 2000 --json /tmp/svc.json
```

> ⚠️ **两次身份必须用同一个 `--top` 和同一个 filter**，否则数字不可比
> （踩过：一次 `top=2000` 一次 `top=1000`，结论直接反了）。


## 🔑 收票单权限怎么配（回答"员工可见有流水号的池票，但看不到同事的"）

> 前提：金蝶的权限体系 WebAPI **不可读**（实测 `SEC_Role` 报"业务对象不存在"，
> `SEC_User` / `SEC_DataRule` 报"**您没有…的查看权限**" —— 表单存在但无权访问），
> **所以这项配置只能在金蝶 UI 里做，agent 无法代劳**。

### 权限的三个层次（截图那页对应的是第 2 层）

| 层 | 位置 | 控制什么 | 你要改的 |
|---|---|---|---|
| 1. **功能权限** | 角色 → 业务对象 → 功能权限 | 能不能**打开**收票单、能不能"查询/查看/新增/修改" | `查询`+`查看` 给 `是`（截图里已是 `是`） |
| 2. **数据权限（数据规则）** | 角色 → 基础资料权限 → 数据规则 | 能看到**哪些行** | ⬅ **「只能查看自己单据」就要改这里** |
| 3. **字段权限** | 同页的列（有权限/无权限/禁止/数据范围） | 能看到**哪些列** | 建议隐藏金额/税号等敏感列 |

### 精确做法：**新增一个角色，别改现有角色**（推荐）

金蝶**多角色权限取并集**，所以这样最安全、可回退：

1. 新建角色 `发票池-只读`（角色类型：普通角色）。
2. 「功能权限」子系统中勾 `发票管理 / 收票单` → 只给 **查询 + 查看**（**不给**新增/修改/删除/提交）。
3. 「基础资料权限」里给收票单挂一条**数据规则**：
   ```
   FCreatorId  =  <归集账号的内码，如 16394>
   ```
   （若金蝶版本的数据规则支持"或"，也可写成 `FCreatorId = 16394 或者 FCreatorId = 当前用户`。）
4. 把该角色**追加**给员工（员工原有角色**不动**，它仍管着"只能查看自己单据"）。
5. 净效果：员工可见 = **自己建的票** ∪ **归集账号建的票**，**看不到其他员工手工建的票** ✅

> ⚠️ **实测已验证这个警告是真的**（2026-09-22）：把「只能查看自己单据」**直接关掉**之后，
> 钱八看到的是**组织 101 下 18 个制单人的 2000 张票**（含同事私有票，且能 `View` 打开同事的单），
> 而不是"自己的 + 归集账号的"。
> 所以：① 优先**新增角色**（加法），别改现有角色（减法）；
> ② 数据规则的比较值用**归集账号内码 `16394`**，不是"全部"；
> ③ 条件必须能表达"**或**"：`FCreatorId = 16394 或者 FCreatorId = 当前用户`
>    —— 若该版本数据规则做不出"或"，**路线2 直接不成立，请走路线1**。

### 但请先想清楚这 3 个坑（决定要不要走这条路）

1. **池票没有"归属"标注** —— 归集账号建的 635 张票，系统里**没有字段说明哪张是哪个员工的**。
   员工只能靠发票号搜。如果他手上没有号码（发票还在自己手机里），就得一张张翻。
2. **票会被抢** —— 两名员工都能看到同一张票，都能挂。金蝶**不校验发票号重复**（实测同号能建第二张），
   于是**同一张票可能被挂到两张报销单** → 财务必须人工查重。这是真实风险。
3. **仍然没省掉一次操作** —— 员工还是要自己进金蝶选票。

⇒ **建议**（2026-09-22 定稿）：
- **日常主路 = 路线1**（中央 agent + 服务账号，员工只把发票丢进对话，前端看不到池子）；
- 上面的权限配置只作为**路线2 备选**，而且要**先确认数据规则能否写"或"**；
- ❌ **不要把服务账号下发到员工电脑**（路线3，已否决）；
- ❌ **不要把"只能查看自己单据"整个关掉**（实测=整组织可见，同事私有票全露）。

## 🔴 跨组织报销：**禁止**（2026-09-15 实测，两次结论修正后的定稿）

**问题**：能不能把 A 组织的发票挂到 B 组织的报销单上？（例：购方「示例科技」的票，给「示例四科技」/「示例二科技」报销）

**结论：不能。而且是"能写进去、单据却不可用"的那种不能。**

> ⚠️ **先分清两个动作，别把"建收票单成功"当成"单据可用"**（2026-09-22 用户追问后补）：
> - **建收票单** = `Save IV_ReceivedInvoice`，往**收票池**加一张票。这一步成功**只代表票进池了**，与报销单无关。
> - **挂票** = `Save FRecInvInfo`，把票关联到某张报销单的收票信息。
> - **单据可用** = 界面/审核时按**本组织税号**能从发票云取到这张票的流水号。
> 三者是**三个独立关卡**：建票成功 + 挂票成功，仍可能在第三关被判 `D`。
> **只有跨组织**（票购方 ≠ 报销单组织）才会卡在第三关；同组织的票走完三关没问题。

实测三轮，结论逐轮修正（过程留档，避免再走回头路）：

| 轮次 | 看到的现象 | 当时的错误结论 |
|---|---|---|
| ① | `Save` 写 `FRecInvInfo` 成功、收票单联动回写 | "能挂" |
| ② | `Submit` 也成功，当场读到 `B` | "接口完全接受" |
| ③ | 随后变 `D`；**界面点「查看发票」直接报错** | ✅ **真结论：不可用** |

**界面上真实报的错**（用户实测截图）：
```
驳回：无法获取当前关联收票单的发票云发票流水号，请尝试删除收票单后重做收票
```

**为什么能写进去 —— 我走的是官方没提供的那条路，不是"绕过后门"**

1. **金蝶 WebAPI 根本没有"补发票"这个 Operation**（`ER_ExpReimbursement` 的 18 个 Operation 里没有）。
   发票云取票 + 回写流水号是**纯 UI 交互**，WebAPI 没开放。
2. 所以 WebAPI 挂票只能**直接 Save `FRecInvInfo` 子表** —— 等于写一行"指向某张收票单"的
   关联记录，**完全跳过发票云的取票与组织归属校验**。
3. 我们的 `Save` 报文还额外把校验关掉了：`ValidateFlag=false` + `IsVerifyBaseDataField=false`
   （`recvin_link.py::save()` 的默认值）→ 连"收票单组织 ≠ 报销单组织"这类字段级校验也不会拦。
4. 于是校验被**推迟到界面打开 / 审核环节**才发生 →
   表现就是「Save ✅ → Submit ✅（B）→ 界面/审核 ❌ → `D`」。

**三条硬约束（`guard_recv_invoices()` 已内置，写入前拦截）**

| # | 约束 | 违反后的表现 |
|---|---|---|
| 1 | 收票单 `SOURCEORGID`（购方/来源组织）**必须 = 报销单组织** | 界面报「无法获取…发票云发票流水号」→ `D` |
| 2 | 收票单必须是**发票云归集**的（`FPDFURL` / `FPIAOZONESERIALNUMBER` 非空） | 同上（同组织也会报） |
| 3 | 目标组织**收票服务许可**须在有效期内 | 发票云报「…【收票服务】许可已过期失效…[0300]」 |

本次两张单各踩了不同的坑，正好把三条都验证到：

| 报销单 | 组织 | 收票单 | 组织归属 | 发票云流水号 | 实际报错 |
|---|---|---|---|---|---|
| 100005 | 104 | SPD00000002 | ❌ 属 101 | ❌ **空**（非发票云归集） | 「无法获取…发票云发票流水号」 |
| 100006 | 105 | SPD00000001 | ❌ 属 101 | ✅ 有（`GENERATETYPE=4`） | 「【收票服务】许可已过期失效 [0300]」 |

→ 两张单的失败**都可以归到"票不是本组织的"**：
第 3 条（105 许可过期）是**叠加**上去的独立故障 —— 就算票是 105 自己的，105 现在也收不了票。

**⚠️ 挂票会改写收票单，撤销时要还原**

收票单有 `SOURCEORGID`（来源/购方组织）与 `SETTLEORGID`（结算组织）。挂票后
`SETTLEORGID` 会被**改成目标报销单的组织**，同时 `LINKBILLID/LINKIVNUMBER/LINKBILLTYPE`
回写指向那张报销单：

| 收票单 | SOURCEORGID / PURNAME | SETTLEORGID 原 | 被挂后 | LINK 字段 |
|---|---|---|---|---|
| SPD00000002 | 101 / 示例科技有限公司 | 101 | **104** | → 100005 / FYBX…0001 |
| SPD00000001 | 101 / 示例科技有限公司 | 101 | **105** | → 100006 / FYBX…0002 |

→ **挂错了要还原：`SETTLEORGID` 改回原组织 + 清掉 LINK 三件套**，
否则这张票在原组织的收票核算里也是错的。

### ⚠️「没有购买发票模块」≠ 收票信息不可用（此结论仍成立，但它不是"能挂"的理由）
示例二科技（org 105）据称未购买发票模块，但：
- 收票信息写入 **成功**（1 行，无任何报错），提交 **A→B 也成功**
- 收票单在 `CreateOrgId` = 0 / 101 / 104 / 105 下**均可见**，没有可见性拦截
- **反向证据**：org 105 名下（`SOURCEORGID=105`）有 **701 张**收票单，
  `SETTLEORGID=105` 的有 542 张 → 105 本身就有大量收票单，"没有发票模块"在**收票单层面不成立**

**正确理解**：发票模块影响的是**发票自动归集**（发票云把票自动收进收票池 → 自动生成收票单），
**不是**报销单「收票信息」这个子表本身。所以：
- 没有发票模块的组织 → 它的发票**不会自动进收票池**，需要人工建收票单或用附件
- 但"能挂上"有前提：**收票单必须是本组织的**（见上面三条硬约束）。
  ~~哪怕是别的组织建的也能挂~~ —— 这句只对 `Save` 成立，对**单据可用性**不成立。

### 🔑 那为什么"示例二科技要走附件"是对的？—— 是**业务做法**，不是系统限制
历史单据实测（2026-09-15）：

| 报销单 | 组织 | 状态 | 收票信息行数 |
|---|---|---|---|
| 历史单A (100007) | **105 示例二科技** | C 已审核 | **0 行** |
| 历史单B (100009) | 104 示例四科技 | B 审核中 | 1 行 |
| FYBX20260101000002 (100004) | 101 示例科技 | C 已审核 | 2 行 |

→ **org 105 的历史报销单根本不用收票信息**（0 行照样审核通过），org 104/101 才用。

**结论**：
- 技术层面「能挂」≠ 业务层面「该挂」。
- 对 **105 这类不用收票信息的组织**，应**跟随其既有做法走附件**，
  硬挂收票信息反而会与历史单据口径不一致（且会把来源组织的收票单改写成 105 结算）。
- skill 的默认动作：**先查该组织历史报销单用不用收票信息**，再决定走「收票信息」还是「附件」。

### 🔴 硬拦截：`发票购方名称 ≠ 报销单组织名称` ＝ **不许挂**
这是**税务归属**问题（谁的进项、谁抵扣），而且系统在界面/审核环节也会挡。
实测两张票购方都是「示例科技有限公司」，而报销组织是「示例四科技有限公司」/「示例二科技有限公司」。
→ **这是阻断项，不是"提醒一下再确认"**。`recvin_link.py link / precheck` **硬拦**；
   v2.0.11 起 `--allow-cross-org` 已删除，**越过不去**。

### ✅ 拦截落在两道口子上（2026-09-15 补测，v2.0.11 收紧）
```
recvin_link.py link    <FID> <收票单号…>     → 写入前就拦（避免产生坏数据）
recvin_link.py submit  <FID>                 → 提交前再拦一次（cmd_submit 先跑 precheck）
```
- `link` 拦截点：`guard_recv_invoices()`，**在 Save 之前**返回失败，**不会写库**。
- `submit` 拦截点：`cmd_submit` → `precheck()`，**有阻断项直接 return 2，连 `Submit` 接口都不调**。
  实测输出「⛔ 体检有阻断项，已中止」并列出跨组织 + 无发票云流水号两条，
  **没有发起任何提交**（v2.0.11 起提示语里不再出现任何"强提交"参数）。
- ⚠️ 也就是说：**正常走 skill 的路径，这种单现在"提交不过去"了**，
  而且 **v2.0.11 起没有任何 CLI/库参数可以越过**（`--force` / `--allow-cross-org` /
  `--allow-no-piaozone` / `--allow-no-invoice` 全部删除）。

### ⚠️ 别把「收票信息为空」当阻断项（2026-09-15 修正）
曾经把它列为 blocks，理由是"大概率报发票金额不允许小于报销金额"——**实测推翻**：
`100007`（org 105）**报销金额 金额以实际单据为准、收票信息 0 行**，照样 Submit 成功并走到 `C` 已审核。
→ 已降级为 warn。否则会把 org 105 这类"走附件"的正常单据误拦。

### 📌 提交后的状态流转（D 的真正成因已查明）
- `Submit` 返回成功，**当场读到的状态 = B（审核中），且收票信息保持** ✅
- ⚠️ 但随后再读，两张单都变成了 **`D`**，且 `APPROVERID` / `APPROVEDATE` 为空。
- **`D` 的权威含义 = 「审核驳回」**（金蝶官方《单据状态通用说明》）：
  > 「重新审核：用户执行业务审核**驳回**操作时单据状态更新为重新审核，表示当前单据审核**不通过**，需要重新审核。」
  > —— https://open.kingdee.com/K3Cloud/help/PUR_NewTopic43.html
  - 该状态**类同「创建」**：可以修改、可以删除、可以再次提交；但不可下推、不被其他单据引用。
  - 另一处口径「已审核后被反审核」在本次场景**不适用**（这两张单从未到过 `C`）。
- **成因不是"人工审批不通过"，而是发票云取不到这张票的流水号**。
  ⚠️ 触发点要写准（用户 2026-09-15 澄清）：**报错不是在"审核"动作里出现的，而是在
  「单据被驳回后、在收票信息页签重新上传/补发票」时出现的**：
  `驳回：无法获取当前关联收票单的发票云发票流水号，请尝试删除收票单后重做收票`。
  也就是说 —— 只要界面要按**本组织税号**去发票云取这张票（打开发票、补发票、重新上传、
  后续审核/结算），都会取不到。`D` 同理：金蝶把"操作被拒绝"写作"驳回"，
  所以 `APPROVERID` 一直是空的 —— **根本没有审批人参与**。
- **全环境基线**（2026-09-15 取 300 张报销单）：`C`×297、`B`×1（100009 长期卡住）、
  `D`×2（正好就是本次跨组织测试的 100005 / 100006）。
  → **跨组织挂票是本环境唯一出现 `D` 的单**，其余走 org 自己的票都能到 `C`。

**所以真正的风险落点不是「提交失败」，而是「提交成功、但单据在界面里没法继续走」**：
- WebAPI 层面的硬校验（金额 ≤ 发票价税合计、联动表合计、往来单位）**全部通过**，
  `Submit` 照常回 `IsSuccess=true` —— **接口不会替你挡这个错**。
- 因此拦截必须**由 skill 自己做**（见上「拦截落在两道口子上」），不能指望金蝶接口返回失败。
- 提示原文/流程节点**只在 UI 里看得到，WebAPI 读不到**（`WF_*` / `BOS_OperationLog`
  等表单均不存在，别去猜）。
- 因此遇到 `D` **必须让用户去 UI 看提示原文**，**不要自行重提交**、更不要靠重试硬撞。
- 状态口径参考：`Z` 暂存 / `A` 创建 / `B` 审核中 / `C` 已审核 / `D` 重新审核（驳回）

### ⚠️ 两条复核项（本次实测顺带发现）

1. **同一张发票不要既挂「收票信息」又传「附件」**（100006 就是这种情况：收票信息 1 行
   `SPD00000001` + 同一张票的图片附件）。两个入口都会被财务看到，口径上算 **重复佐证**，
   应二选一。默认：**能挂收票信息就只挂收票信息**；只有该组织既有做法是走附件（如 org 105）才走附件。
2. **收票单上的联查号可能是「悬空号」**：`SPD00000002` 的 `FLINKIVNUMBER = CLFBX00000001`，
   但该差旅报销单**已不存在**（某次下推占号后未保存/被删）。
   → 挂票前若看到 `FLINKIVNUMBER` 有值，**先按单号反查该单是否真的存在**，
   别把它当成"这张票已经被用掉了"而误判。
   （反向也成立：反查单据用 `ExecuteBillQuery` + `FBillNo='...'` 精确匹配，
   返回空数组 `[]` 就是不存在，不会报错。）

## 发票处理（收票单创建 + 附件分流 + 行程单预警）

1. **收文件先做分类**（调用 `helpers/invoice_classifier.py`）：
   - **发票**：有发票号码、购销方税号、价税合计、税率等要素。
   - **行程单**：机票行程单（航班号、出发/到达机场、旅客姓名、票价/税费/总金额）；火车票（车次、出发/到达站、席别）；住宿清单等。
2. **发票** → **先查收票池**（`IV_ReceivedInvoice`，用 `recvin_link.py find <发票号>`），查到就直接挂收票信息；
   查不到才用 Save 建收票单 `IV_ReceivedInvoice`（字段见 cookbook §3）。
3. **行程单** → 传**附件**（`UploadFile`+`BOS_Attachment`），并提醒用户"行程单请传附件，避免与发票重复计入导致汇总金额虚高被财务驳回"。
4. **前置校验**（提交系统前先做，不合规打回）：
   - 清晰度：模糊/缺角/反光/非发票 → 打回重拍。
   - 购方抬头：OCR 提取购方名称，须 = 申请组织名称（如示例科技有限公司）；不一致拦截并说明。
   - 金额匹配：发票价税合计应与报销金额匹配（允许合理差异）。

### ⚠️ 行程单误传收票信息 → 金额双算预警（必须实现）

若用户坚持把**行程单**传到收票信息（而非附件）：系统 OCR 也会识别金额，导致同一笔业务被"发票+行程单"重复计入，汇总金额虚高。

处理：
- **识别到行程单却要传收票信息** → 先提示风险，计算并展示**当前汇总金额**给用户，明确警告"可能被财务驳回"；用户仍坚持则照传（记录日志）。
- 优先引导：行程单走附件，发票走收票信息。

## 报销制度提醒（**制度不内置 —— 模板留位，由使用方自填**）

- ⚠️ **各公司制度不同，本 skill 不内置任何具体限额 / 审批要求**。模板见 `references/policy_rules.md`，
  里面全是【待填】空位（住宿限额、招待费审批、报销时限、税号…）。
- 使用方把**本公司规则**写到 **`references/policy_rules.local.md`**（已列入 `.gitignore` /
  `.clawhubignore`，不会进公开仓库）；agent **优先读它**，读不到就只按通用口径处理。
- 🔴 **通用部分（与实现绑定、任何公司都适用，不算"制度"）只有两块**，已固化在模板里：
  ① 金额与发票一致性口径（`fit` 调低；只能回源单加额度）；
  ② 行程单 vs 发票的双算预警（行程单一律传附件）。
- 🔴 模板里写【待填】的项，agent 必须**明确说"贵司这项还没登记"**，
  **绝不许拿别家数值或"常见做法"当默认** —— 编一个限额出来，比不提醒更糟。

## 流程状态仪表板（规划）

- 定时查询各单据 `FDocumentStatus`（A暂存/B已提交/C已审核/D 等）+ 下推/付款关联，落本地 SQLite。
- 按人/按单/按状态分组，生成 HTML 仪表板，每日推企业微信（复用现有推送机制）。
- 状态口径：待提交 / 审核中 / 已通过 / 已报销 / 待打款。

## 已知环境坑（务必先看 references/field_cookbook.md）

- **`Save` 字段必须带 `F` 前缀**：`View` 返回的 `SUMALLAMOUNT`/`ExpenseAmount` 等，Save 时要写成 `FSUMALLAMOUNT`/`FExpenseAmount`，否则被静默忽略。
- **报销单 `FCONTACTUNIT`(往来单位)**：下推后为空，Submit 前必须局部更新。Save 键为 `FCONTACTUNIT`/`FCONTACTUNITTYPE`，引用用 `{"FNumber":"员工号"}` 或 `{"Id":员工Id}`。
- **✅ 收票信息 `RecInvInfo` 可以用纯金蝶 WebAPI 写入（2026-09-14 打通，此前"写不进"的结论是错的）**：
  `Save ER_ExpReimbursement`，`Model={"FID":<FID>,"FRecInvInfo":[{"FRecInv":{"FBillNo":"<收票单号>"}}]}`。
  实测与官方金税互联插件写入结果逐字段一致，收票单侧 `LINKBILLID/LINKIVNUMBER` 自动回写。
- **`FRecInv`(收票单) 元数据标着 `IsNewLock=IsEditLock=True`，但它照样能被 WebAPI Save 写入**。
  ⚠️ **不要只凭 `IsNewLock/IsEditLock` 就断定某字段"写不进"** —— 这是本次绕了远路的根源，必须实测。
- **字段层级是历史 7 种写法全失败的真凶**：`FRecInv`/`FIVSerialNo` 必须放在 `Model.FRecInvInfo[]`
  **数组行内**。放到单据头层级会明确报错 `…实体不存在此属性！[EntityType：BillHead Propeyt…]`
  —— 看到 `[EntityType：BillHead]` 就是层级放错了。
- **`FIVSerialNo`(发票序列号) 不必写**：官方流程产物的 `FIVSERIALNO` 实测是空的（100003/100004 均为 `" "`），
  承载关联的是 `FRecInv`。它是可选装饰，不是入口。
  （顺带：它的正确值若真要填，是收票单 `FPDFURL` 尾部 **32 位** hash，**不是** `FPIAOZONESERIALNUMBER` 那个 33 位。）
- **防抢关联**：一张收票单只能属于一张报销单。把它挂到第二张单上会**静默改掉**原单的关联，
  造成"原单还列着这张票、票却说属于别人"的不一致。写之前先看收票单的 `FLINKBILLID`；
  `recvin_link.py` 默认拦截，`--allow-steal` 才放行。
- **已审核(C)单据的收票信息同样可 Save 改**（不报错）→ 操作前务必先判单据状态。
- **收票单必须先存在，且必须有发票云流水号**：v2.0.11 起这两条都是**硬闸门**。
  「发票号码查不到收票单」的常见原因：① **这张票从没被采集过**（不是"系统会自动补"——
  每日"下载"要电子税务局手机验证码，无法自动化）；② 号码错；③ 属其它组织（用**有权限的账号**再查一遍）。
  ✅ **正确动作：让员工在报销软件里把这张票上传一次**，当天进池、当天有流水号。
  ❌ 不要手工建票（`FGENERATETYPE` 为空 = 永远没有流水号 = 提交必被拦）。
- **`ExecuteBillQuery` 字段名与 View 不同且写错就报错**：View 里叫 `PDFURL`/`PURNAME`，查询要写
  `FPDFURL`/`FPURNAME`；另外**返回列顺序 = `FieldKeys` 顺序**（实测），可安全按位置解析。
- **判定"某字段能不能写"**：`QueryBusinessInfo`（见 cookbook §4.2）能拿到 `Entrys[]/Operations[]` 结构，
  用来**看结构、拿实体名**很有用；但可写性**不能只看 IsNewLock/IsEditLock**，要实测。
  注意 **View 用 `EntryName`（`RecInvInfo`），Save 用 `Key`（`FRecInvInfo`）**。
- **标准库直连金蝶必须带 CookieJar**：登录后靠会话 Cookie 认身份，裸 `urllib.urlopen` 会拿到
  「会话信息已丢失，请重新登录」，且 `View` 返回空字典**不报错**（极易误判成"单据没数据"）。
- **编辑锁冲突（"…业务操作-\"[费用报销单-FYBX…-修改]\"冲突，请稍候再使用。"）是瞬时的，重试即可**：
  实测同一脚本内重试 1 次即成功（`recvin_link.py` 已内置自动重试）。
  ⚠️ 旧笔记写的"需客户端关闭或重下推新单"**是过度结论，别照做**。
- **收票单 `IV_ReceivedInvoice` 的 Save Model 是「扁平结构」**：表头字段（`FIVNUMBER`/`FOPENDATE`/
  `FSUMAMOUNT`/`FSUMTAXAMOUNT`/`FSUMALLAMOUNT`/`FPURNAME`/`FSALENAME`…）**直接写在 Model 根**，
  **不要**用 `FBillHead` 包裹 —— 包了会 `IsSuccess=true` 但表头字段**全部静默丢弃**（只剩分录行）。
  官方《收票单》文档的 Model 示例就是扁平写法。
- **更新收票单分录要 `IsDeleteEntry=true`**：不带 `FEntryID` 直接再 Save 一次会**追加**分录（2 行变 4 行），
  必须用 `IsDeleteEntry="true"` 清旧重建；该开关只清分录，**不动表头**。
- **`NeedUpDateFields` 与「新增分录行」互斥**：指定 `NeedUpDateFields` 后，Model 里**其它实体的新行
  （如联动表 `FReimbAndRecInvInfo`）会被静默丢弃**。改已有字段用一次 Save（带 NeedUpDateFields），
  新增分录行**必须另起一次 Save**（不带）。
- **`ValidateFlag="true"` 时 `FCONTACTUNIT`(往来单位) 是必填**，不填 MsgCode=11 报
  「字段"往来单位"是必填项」。`recvin_link.py` 的 `save()` 默认 `ValidateFlag=false` 所以不报 ——
  但**别因此以为往来单位可选**。规律：`FCONTACTUNIT` = **申请人本人**（= `ProposerID`，官方单据实测一致），
  类型 `BD_Empinfo`，引用用 `{"FNumber":"员工号"}` 或 `{"Id":员工内码}`。
  （Submit 本身不校验它，所以"空着也能提交成功"，但单据不完整。）
- **附件上传两个可用端点 + `IsLast` 必填**：
  - `DynamicFormService.UploadFile.common.kdsvc`：只上传拿 `FileId`。必填 `FileName` + `SendByte`(base64) + **`IsLast:true`**。
  - `DynamicFormService.AttachmentUpLoad.common.kdsvc`：一步上传并挂到单据。必填
    `FileName`/`FormId`/`IsLast`/`InterId`(单据内码)/`BillNO`(单据编号)/`SendByte`。
  - `DynamicFormService.AttachmentDownLoad.common.kdsvc`：回环下载，传 `FileID`，返回 `FilePart`(base64)+`FileSize`+`FileName`。
  - ⚠️ `FileUploadService`、`GetAttachmentList`、`DownloadFile`、`GetFileInfo` **都不存在**；
    `Attachment.AttachmentService` 命名空间也不存在。别浪费时间猜。
  - ⚠️ `BOS_Attachment` 用 `ExecuteBillQuery` 查会报「数据库执行异常」（不是普通可查业务对象），
    **验证附件一律走 `AttachmentDownLoad` 回环比字节**。
  - 报错信息会直接说「data数据包中的参数X必传」→ 想知道契约就发空包读报错，比翻文档快。
- **「合并生成费用明细」没有对应的可调操作**（18 个 Operation 里没有）。它的效果 = 写
  联动表 `FReimbAndRecInvInfo` + 明细侧 `FReimbLinkInvCode`。明细侧那几个字段元数据标着
  `IsNewLock/IsEditLock=True`，但**实测可写**（又一次印证"锁标记 ≠ 写不进"）。
  对照真实样本 `FYBX20260101000003`(FID 100010) 复刻结构最稳。
- **联动表不是必需的**：真实已审核单 100003/100004 的 `FReimbAndRecInvInfo` 就是 0 行，照样审核通过。
  只有走过 UI「联动处理」的单才有（如 100010 有 5 行）。所以别把"联动 0 行"当成提交阻断项。

- **`ExecuteBillQuery` 报错时返回「双层 list」包裹体**（`[[{"Result":{"ResponseStatus":…}}]]`）：
  旧版 `_unwrap_error` 只判 `r[0]` 是不是 dict，遇到嵌套 list 会**静默跳过**，
  于是 `query()` 把**错误对象当成一行业务数据返回**（不报错、不抛异常）。
  典型触发：字段名写错——**给报销单查 `FCostOrgID` 会报「元数据中标识为FCostOrgID的字段不存在」**，
  报销单的组织字段叫 `FOrgID` / `FExpenseOrgId` / `FPayOrgId`（`FCostOrgID` 是**费用申请单**才有）。
  已修（2026-09-15）：先剥掉所有嵌套 list 再判。
- **出口代理会偶发瞬时故障，不是金蝶的问题**（2026-09-15 实测）：
  形态为 `URLError: Tunnel connection failed: 502 Bad Gateway` 或
  `URLError: _ssl.c:1015: The handshake operation timed out`，
  实战中**连错 4 次第 5 次成功**。旧版 `_post` 没有重试，会把网络抖动误报成业务失败。
  已修：`_post` 默认**重试 5 次 / 间隔 6s**。
- 🔴 **发票云会把「行程单」也采集为收票单 —— 必须靠字段识别，不能只看金额**（2026-09-22 实测）：
  阳光出行的**行程单 PDF** 被上传到发票云后生成了收票单（2 张内容完全相同 = 重复上传），
  其特征（View 字段，均无 F 前缀）：
  `INVOICETYPE=11`（未识别类型）、`FIVNUMBER` 为空串或**单个空格 `" "`**、`FSALENAME`/`FPURNAME` 为空、
  `FISELECTRONIC=False`、`ISEXAMINE=3`、`GENERATETYPE=3`；`PDFURL`/`PICTUREURL` 指向 piaozone 的图片/PDF。
  → **挂票前必须核验收票单的「发票号码 + 销方」非空**；金额相同并不代表就是那张发票。
    把它当发票挂上去 = 行程单冒充发票 → 金额双算、财务必驳。
- **`INVOICETYPE` 票种对照（实测）**：`26` 数电普票 / `27` 数电专票 / `29` 铁路电子客票 /
  `11` 未识别（多为行程单或非发票图片）。查收票池时可用它先筛掉"非发票"的收票单。
- 🔴 **收票池查询有「账号可见性」隔离 —— 查票必须用有权限的账号**（2026-09-22 实测，**曾因此得出错误结论**）：
  同一个 `IV_ReceivedInvoice` 池子，**不同账号看到的行数完全不同**：
  | 账号 | 能看到 | 说明 |
  |---|---|---|
  | 普通员工（如「钱八」） | **24 张** | **只看到 `FCreatorId` = 自己**的收票单 |
  | 取数/集成账号 | **1000 张** | 全池 |
  实测：5 张票（`SPD00008766`~`08770`，`FCreatorId=16394`）在**钱八账号下按发票号码查是 0 条**，
  换取数账号立刻查到、且**都带发票云流水号**。
  → **"某张发票查不到"时，先换有权限的账号再查一遍**，别急着判定"发票云没归集"。
  → ⚠️ `recvin_link.py` 的 `find` / `guard_recv_invoices` 用**员工账号**跑会产生**假阴性**：
    `guard` 会以「收票单不存在」**直接阻断**，且该分支**没有 `--force` 逃生口**。
    所以：**体检/挂票用有权限的账号，员工身份只用于最终提交。**
- 🔴 **手工建收票单换不来发票云流水号 —— 即使信息全对、组织一致**（2026-09-22 实测）：
  用 WebAPI `Save IV_ReceivedInvoice` 建一张（发票号/购销方名称+税号/金额/税额/开票日/明细行
  **全部正确**，`FSOURCEORGID` = 报销单组织），Save 回 `IsSuccess=true`、单据正常生成，但：
  | 字段 | 手工建 | 自动归集（对照） |
  |---|---|---|
  | `GENERATETYPE` | `' '`（空） | `3` |
  | `FPIAOZONESERIALNUMBER` | **`' '`（空）** | `7d3d30bd…` |
  | `PDFURL` / `PICTUREURL` / `PICTUREXMLURL` | **全空** | 全有（`api.piaozone.com`） |
  | `ISEXAMINE` | `'0'` | `'1'` |
  → **流水号是「发票云采集这张票时回写」的，不是金蝶按发票要素自己算的。**
    手工建单只是往金蝶插一行，发票云那边一无所知 → **拿不到流水号**。
  → 全池 1000 张收票单里**有流水号的 1000 张**；仅有的 3 张无流水号（`SPD00008633/08634/08775`）
    都是手工建产物，`GENERATETYPE=' '`。
  → ⚠️ 手工建的票**还会占用发票号码**（本环境池里发票号基本无重复），
    可能干扰后续发票云自动归集同一张票。**测完记得删**（员工账号无删除权限，须用有权限账号）。
- ✅ **员工自助的正确路径：把发票送进「发票云」，而不是手工建收票单**（2026-09-22 实证）：
  池里有 2 张 `SPD00008776/08777`——内容是**阳光出行行程单**（非发票，`INVOICETYPE=11`、发票号空），
  但 **`GENERATETYPE=3`、`FPIAOZONESERIALNUMBER` 非空、`FCreatorId` = 钱八本人**。
  → **员工自己的账号把发票文件送进发票云（金蝶 App / 报销单界面「上传发票」/ 发票云 App），
    就会生成「归自己名下 + 带发票云流水号」的收票单**，**不依赖管理员、不依赖电子税务局每日登录**。
  → 这条路径生成的票，员工自己也看得到（`FCreatorId` = 自己），挂票、提交都顺。
  → ⚠️ **发票云 API（票总管）只提供「绑定/缓存/状态」接口，没有「上传发票文件」接口**
    （`helpers/piazzone_tool.py` / `piazzone_client.py` 只有 `getUserKey/getLinkKey/cache/save/status/update/delete/bxdInvoices`）
    → **agent 目前无法程序化把发票文件送进发票云**，这一步只能员工在 UI 完成
    （或改走"手工建收票单"路线，但要接受**无流水号**，见下条实测）。
  → 凭证：`helpers/piazzone_config.py`（本机已有，`prod_a` 示例科技 / `prod_b` 示例二科技，
    `base_url` 默认 `api.piazzone.com`，若被网关按 SNI 拦会自动 failover 到 `api.piaozone.com`）。
    注意**生产域名 `api.piazzone.com` 在公网 DNS 无记录**（IP 52.83.114.130），需 hosts 或 IT 放行。
- ✅ **「同组织 + 无发票云流水号」这个格子已实测：业务校验层面能通**（2026-09-22 实测，**推翻旧推断**）：
  做法：拿一张手工建的收票单（`SPD00008775`，`GENERATETYPE=' '`、流水号空、`SOURCEORGID`=101）
  挂到**同组织 101** 的差旅报销单上，再把明细金额对齐到 ≤ 发票金额，然后跑
  `recvin_link.py strict <FID>`（= `ValidateFlag=true` 的真实业务校验）：
  | 校验项 | 结果 |
  |---|---|
  | 「发票金额不允许小于报销金额！」 | 金额对齐后 **消失**（金额没对齐时会报——这是**唯一**与发票相关的校验） |
  | 「发票云流水号」相关报错 | **完全没有**（`Save` 阶段根本不查流水号） |
  | 剩余报错 | 只有必填/勾稽类（`差旅费类型` 必录、`差旅费金额 ≠ 税额 + 费用金额`），与发票无关 |
  → **流水号不是业务校验项**：`Save` 不校验它，`Submit` 永远回 `IsSuccess=true`。
    「无流水号致 D」的案例全部同时是**跨组织**的 → 真正致 D 的是**跨组织**，不是流水号。
  → 差旅报销单明细必填/勾稽字段（实测键名）：`FTravelType`(+`_Id`, 差旅费类型，必录)、
    **`FTravelAmount`**(差旅费金额) 必须 = `FTaxSubmitAmt`(不含税) + `FTaxAmt`(税额) = 含税，改金额时**要一起改**，
    否则报「差旅费金额不等于税额加费用金额」。
    ⚠️ 键名是 `FTravelAmount`，**不是** `FExpTravelAmount`（旧版记错，2026-09-22 在 Z 单上写入验证后订正；
      `expense_edit.set_detail()` 旧版整个漏写此字段，也已修）。
- 🔴 **把发票 PDF 传到收票单「附件」，不会补发票云流水号**（2026-09-22 实测，**推翻"校验时会自动补号"的推测**）：
  流程：新建收票单（`Save IV_ReceivedInvoice` 回 `IsSuccess=true`）→ `AttachmentUpLoad` 传该票的真 PDF
  （回环 `AttachmentDownLoad` 字节一致、`FileId` 有效）→ 立即 + 等 25 秒各回读一次：
  | 字段 | 传附件前 | 传附件后 |
  |---|---|---|
  | `FPIAOZONESERIALNUMBER` | `' '` | **`' '`（仍空）** |
  | `GENERATETYPE` | `' '` | `' '` |
  | `ISEXAMINE` | `'0'` | `'0'` |
  | `PDFURL` / `PICTUREURL` | `' '` | `' '` |
  → **附件 ≠ 采集**。金蝶不会因为收票单挂了附件就去发票云找同号票、回写流水号；
    流水号只在**发票云采集这张票的那一刻**写入。**别指望"先建单、后补号"。**
- 🔴 **手工建的收票单不校验「发票号重复」——会污染收票池**（2026-09-22 实测）：
  拿一张**发票云里已有**（`SPD00008770`，发票号 `24000000000000000001`）的票号去手工新建，
  `Save` 照样 `IsSuccess=true` 生成 `SPD00008787`，池里出现**两条同号记录**（一条有流水号、一条没有）。
  → 手工建票前**必须先查重**（`find --invoice-no`，且要用有权限账号）；**测完立即删**。
- 🔴 **手工建票丢掉的是「三重财务保护」，不只是流水号**（2026-09-22 归纳，回答"流水号重不重要"）：
  把手工建的票和发票云采集的票对比，差别不在"能不能提交"，而在这三处：
  | 保护 | 发票云采集的票 | 手工建的票 |
  |---|---|---|
  | **防重复报销** | 发票云按 `expenseStatus`（1未用/30在用/60已用）锁票，一张票只能用一次 | ❌ **完全没有** —— 且金蝶**不校验发票号重复**（实测同号能建第二张），同一张票可被建两次、报两次 |
  | **票据数据可信** | 税局原始数据回写（金额/税号/票号不会错） | ⚠️ 靠 agent 从 PDF/图片 OCR 提取 → 错一位数就挂错票 |
  | **验真与状态** | `ISEXAMINE=1`（已查验）、PDF/XML 原件留存 | ❌ `ISEXAMINE=0`，无查验记录、无原件 URL |
  ⇒ 所以：**从"单据能不能提交"看，流水号确实不重要；从"财务内控"看，丢的是防重、可信、验真三件事。**
    要跑"手工建票"路线，必须靠 skill 侧自己补一重防重（见下条），否则就是**制度风险**。
- 🔴 **手工建票必须自己查重（skill 侧的替代保护）**：由于金蝶不拦同号，`link()` 之前务必
  用**有权限账号**按 `FIVNUMBER` 查一遍池子；已存在就别再建（会变成两条同号记录）。
  ⚠️ 且**池里的重复票不会报错、只会静默污染**，事后极难清理 —— 建票前查重是唯一防线。
- 🔴 **单据一旦 `Submit` 进审批流，WebAPI 就删不掉/撤不回了**（2026-09-22 实测）：
  | 尝试 | 结果 |
  |---|---|
  | `Delete`（状态 B） | ❌「只能删除创建，暂存，重新审核状态的数据!」（即只放 `Z`/`A`/`D`） |
  | `UnSubmit` | ❌ 无此服务，返回非 JSON（`JSONDecodeError`） |
  | `UnAudit` | ❌「已关联工作流实例，不支持通过 WebAPI 接口进行传统审批」 |
  → **`ValidateFlag=false` 越狱提交是不可逆的**：单子进流程后只能人工在 UI 驳回/撤销。
    所以「用测试单验证」前要清楚：**测试单基本收不回来**，别在正式账套里随便 Submit。
- **自动归集是「逐票同步」，不是整批投放**（2026-09-22 佐证）：
  同一天（09-20）开的 3 张住宿专票，2 张已自动归集、1 张没有；而曹操出行（吉利优行）的**普票**
  历史上能自动归集（SPD00008225）→ 说明**不是"网约车票/普票不能归集"**。
  开票越久越可能已到位（08-31 的 8 张铁路客票全部到位）。
  ⚠️ 但**排查"查不到"时，第一步永远是换有权限的账号重查**（见上条账号可见性），
  确认真的没归集，再谈"归集进度"或让员工走发票云上传。

## 复用脚本与代码

- **`helpers/recvin_link.py`（本 skill，★核心）**：报销单「收票信息」写入 —— 纯金蝶标准 WebAPI（仅标准库，
  零第三方依赖）。子命令 `find` / `list` / `link` / `clear` / `precheck` / `set-contact` / `submit` / `verify`，
  支持费用报销单与差旅费报销单，内置防抢关联保护 + 编辑锁自动重试。
  凭证走环境变量（`KINGDEE_BASE_URL/ACCTID/USERNAME/PASSWORD`）或 `config.py`，便于分发给其他公司。
- **`helpers/expense_edit.py`（本 skill，★配套）**：报销单「明细 ↔ 发票对齐 + 附件」。子命令
  `attach`（附件上传，含 `IsLast` 坑与回环校验）/ `set-detail`（改明细金额/费用项目/税率）/
  `linkage`（写明细↔发票联动 = 合并生成费用明细）/ `download`（回环下载校验）/ `check`（金额链条体检）。
- `helpers/piazzone_tool.py`（本 skill，可选兜底）：发票云 API，仅在"发票不在收票池、需程序化采集"时才有意义；
  按官方文档它只是**告知发票云**绑定关系，不回写金蝶。已内置域名 failover。
- `helpers/perm_probe.py`（本 skill，**只读**）：**数据权限可见性实测器** ——
  `--pool`（可见规模 + 按制单人/组织分布 + 判定）、`--by-invoice` / `--by-bill`（定点回查）、
  `--bills`（报销单是否被连带放开）、`--fields`（字段探活）、`--json`（落盘做两次身份对照）。
  排查「查不到票」和上线前验收权限配置都用它。
- `helpers/invoice_classifier.py`（本 skill）：发票 vs 行程单分类、字段提取、双算预警。
- `references/field_cookbook.md`：费用申请单 / 出差申请单的 Save 模板（含全部必填字段）。
下推：`Push` 接口，`formid` 填源单表单、`TargetFormId` 填目标表单（如 `ER_ExpReimbursement`）。
新建收票单：标准 `Save IV_ReceivedInvoice`，Model 用**扁平结构**（见 cookbook §3.3）。
- `官方接口说明/`：金蝶 WebAPI 官方原始文档（Save / Submit / 收票单）。
